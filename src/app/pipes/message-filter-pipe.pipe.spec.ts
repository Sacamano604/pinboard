import { MessageFilterPipePipe } from './message-filter-pipe.pipe';

describe('MessageFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MessageFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
